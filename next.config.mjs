/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['encrypted-tbn0.gstatic.com','www.format.com', "i.pinimg.com","cdn.pixabay.com", "thumbs.dreamstime.com"],
      },
};

export default nextConfig;
